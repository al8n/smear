"Nested map values"

input AdvancedConfig {
  database: <
    String! => <String! => String!>!
  >! = map {
    "credentials" => map {
      "username" => "admin"
      "password" => "secret"
    }
  }
}
