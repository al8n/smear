schema {
  query: Query
}

scalar DateTime

interface Node {
  id: ID!
}

type Query implements Node {
  id: ID!
  node(id: ID!): Node
}

union Any = Query | Node

enum Colour {
  RED
  GREEN
}

input Filter {
  limit: Int = 10
}

directive @d on FIELD
